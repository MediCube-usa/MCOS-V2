# Port the gateway listens on. This does NOT need to match OurVend's port (4150) —
# it's just what you'll type into the machine's "Port number" field once this is deployed.
PORT=4150

# Where captured data is stored (see README — this is a temporary flat-file store
# for the test phase, not the long-term data layer).
# DATA_FILE=./data/store.json
